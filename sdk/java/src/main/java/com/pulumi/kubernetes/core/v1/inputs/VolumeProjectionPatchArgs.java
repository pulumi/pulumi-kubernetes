// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.kubernetes.core.v1.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.kubernetes.core.v1.inputs.ClusterTrustBundleProjectionPatchArgs;
import com.pulumi.kubernetes.core.v1.inputs.ConfigMapProjectionPatchArgs;
import com.pulumi.kubernetes.core.v1.inputs.DownwardAPIProjectionPatchArgs;
import com.pulumi.kubernetes.core.v1.inputs.PodCertificateProjectionPatchArgs;
import com.pulumi.kubernetes.core.v1.inputs.SecretProjectionPatchArgs;
import com.pulumi.kubernetes.core.v1.inputs.ServiceAccountTokenProjectionPatchArgs;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


/**
 * Projection that may be projected along with other supported volume types. Exactly one of these fields must be set.
 * 
 */
public final class VolumeProjectionPatchArgs extends com.pulumi.resources.ResourceArgs {

    public static final VolumeProjectionPatchArgs Empty = new VolumeProjectionPatchArgs();

    /**
     * ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field of ClusterTrustBundle objects in an auto-updating file.
     * 
     * Alpha, gated by the ClusterTrustBundleProjection feature gate.
     * 
     * ClusterTrustBundle objects can either be selected by name, or by the combination of signer name and a label selector.
     * 
     * Kubelet performs aggressive normalization of the PEM contents written into the pod filesystem.  Esoteric PEM features such as inter-block comments and block headers are stripped.  Certificates are deduplicated. The ordering of certificates within the file is arbitrary, and Kubelet may change the order over time.
     * 
     */
    @Import(name="clusterTrustBundle")
    private @Nullable Output<ClusterTrustBundleProjectionPatchArgs> clusterTrustBundle;

    /**
     * @return ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field of ClusterTrustBundle objects in an auto-updating file.
     * 
     * Alpha, gated by the ClusterTrustBundleProjection feature gate.
     * 
     * ClusterTrustBundle objects can either be selected by name, or by the combination of signer name and a label selector.
     * 
     * Kubelet performs aggressive normalization of the PEM contents written into the pod filesystem.  Esoteric PEM features such as inter-block comments and block headers are stripped.  Certificates are deduplicated. The ordering of certificates within the file is arbitrary, and Kubelet may change the order over time.
     * 
     */
    public Optional<Output<ClusterTrustBundleProjectionPatchArgs>> clusterTrustBundle() {
        return Optional.ofNullable(this.clusterTrustBundle);
    }

    /**
     * configMap information about the configMap data to project
     * 
     */
    @Import(name="configMap")
    private @Nullable Output<ConfigMapProjectionPatchArgs> configMap;

    /**
     * @return configMap information about the configMap data to project
     * 
     */
    public Optional<Output<ConfigMapProjectionPatchArgs>> configMap() {
        return Optional.ofNullable(this.configMap);
    }

    /**
     * downwardAPI information about the downwardAPI data to project
     * 
     */
    @Import(name="downwardAPI")
    private @Nullable Output<DownwardAPIProjectionPatchArgs> downwardAPI;

    /**
     * @return downwardAPI information about the downwardAPI data to project
     * 
     */
    public Optional<Output<DownwardAPIProjectionPatchArgs>> downwardAPI() {
        return Optional.ofNullable(this.downwardAPI);
    }

    /**
     * Projects an auto-rotating credential bundle (private key and certificate chain) that the pod can use either as a TLS client or server.
     * 
     * Kubelet generates a private key and uses it to send a PodCertificateRequest to the named signer.  Once the signer approves the request and issues a certificate chain, Kubelet writes the key and certificate chain to the pod filesystem.  The pod does not start until certificates have been issued for each podCertificate projected volume source in its spec.
     * 
     * Kubelet will begin trying to rotate the certificate at the time indicated by the signer using the PodCertificateRequest.Status.BeginRefreshAt timestamp.
     * 
     * Kubelet can write a single file, indicated by the credentialBundlePath field, or separate files, indicated by the keyPath and certificateChainPath fields.
     * 
     * The credential bundle is a single file in PEM format.  The first PEM entry is the private key (in PKCS#8 format), and the remaining PEM entries are the certificate chain issued by the signer (typically, signers will return their certificate chain in leaf-to-root order).
     * 
     * Prefer using the credential bundle format, since your application code can read it atomically.  If you use keyPath and certificateChainPath, your application must make two separate file reads. If these coincide with a certificate rotation, it is possible that the private key and leaf certificate you read may not correspond to each other.  Your application will need to check for this condition, and re-read until they are consistent.
     * 
     * The named signer controls chooses the format of the certificate it issues; consult the signer implementation&#39;s documentation to learn how to use the certificates it issues.
     * 
     */
    @Import(name="podCertificate")
    private @Nullable Output<PodCertificateProjectionPatchArgs> podCertificate;

    /**
     * @return Projects an auto-rotating credential bundle (private key and certificate chain) that the pod can use either as a TLS client or server.
     * 
     * Kubelet generates a private key and uses it to send a PodCertificateRequest to the named signer.  Once the signer approves the request and issues a certificate chain, Kubelet writes the key and certificate chain to the pod filesystem.  The pod does not start until certificates have been issued for each podCertificate projected volume source in its spec.
     * 
     * Kubelet will begin trying to rotate the certificate at the time indicated by the signer using the PodCertificateRequest.Status.BeginRefreshAt timestamp.
     * 
     * Kubelet can write a single file, indicated by the credentialBundlePath field, or separate files, indicated by the keyPath and certificateChainPath fields.
     * 
     * The credential bundle is a single file in PEM format.  The first PEM entry is the private key (in PKCS#8 format), and the remaining PEM entries are the certificate chain issued by the signer (typically, signers will return their certificate chain in leaf-to-root order).
     * 
     * Prefer using the credential bundle format, since your application code can read it atomically.  If you use keyPath and certificateChainPath, your application must make two separate file reads. If these coincide with a certificate rotation, it is possible that the private key and leaf certificate you read may not correspond to each other.  Your application will need to check for this condition, and re-read until they are consistent.
     * 
     * The named signer controls chooses the format of the certificate it issues; consult the signer implementation&#39;s documentation to learn how to use the certificates it issues.
     * 
     */
    public Optional<Output<PodCertificateProjectionPatchArgs>> podCertificate() {
        return Optional.ofNullable(this.podCertificate);
    }

    /**
     * secret information about the secret data to project
     * 
     */
    @Import(name="secret")
    private @Nullable Output<SecretProjectionPatchArgs> secret;

    /**
     * @return secret information about the secret data to project
     * 
     */
    public Optional<Output<SecretProjectionPatchArgs>> secret() {
        return Optional.ofNullable(this.secret);
    }

    /**
     * serviceAccountToken is information about the serviceAccountToken data to project
     * 
     */
    @Import(name="serviceAccountToken")
    private @Nullable Output<ServiceAccountTokenProjectionPatchArgs> serviceAccountToken;

    /**
     * @return serviceAccountToken is information about the serviceAccountToken data to project
     * 
     */
    public Optional<Output<ServiceAccountTokenProjectionPatchArgs>> serviceAccountToken() {
        return Optional.ofNullable(this.serviceAccountToken);
    }

    private VolumeProjectionPatchArgs() {}

    private VolumeProjectionPatchArgs(VolumeProjectionPatchArgs $) {
        this.clusterTrustBundle = $.clusterTrustBundle;
        this.configMap = $.configMap;
        this.downwardAPI = $.downwardAPI;
        this.podCertificate = $.podCertificate;
        this.secret = $.secret;
        this.serviceAccountToken = $.serviceAccountToken;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(VolumeProjectionPatchArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private VolumeProjectionPatchArgs $;

        public Builder() {
            $ = new VolumeProjectionPatchArgs();
        }

        public Builder(VolumeProjectionPatchArgs defaults) {
            $ = new VolumeProjectionPatchArgs(Objects.requireNonNull(defaults));
        }

        /**
         * @param clusterTrustBundle ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field of ClusterTrustBundle objects in an auto-updating file.
         * 
         * Alpha, gated by the ClusterTrustBundleProjection feature gate.
         * 
         * ClusterTrustBundle objects can either be selected by name, or by the combination of signer name and a label selector.
         * 
         * Kubelet performs aggressive normalization of the PEM contents written into the pod filesystem.  Esoteric PEM features such as inter-block comments and block headers are stripped.  Certificates are deduplicated. The ordering of certificates within the file is arbitrary, and Kubelet may change the order over time.
         * 
         * @return builder
         * 
         */
        public Builder clusterTrustBundle(@Nullable Output<ClusterTrustBundleProjectionPatchArgs> clusterTrustBundle) {
            $.clusterTrustBundle = clusterTrustBundle;
            return this;
        }

        /**
         * @param clusterTrustBundle ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field of ClusterTrustBundle objects in an auto-updating file.
         * 
         * Alpha, gated by the ClusterTrustBundleProjection feature gate.
         * 
         * ClusterTrustBundle objects can either be selected by name, or by the combination of signer name and a label selector.
         * 
         * Kubelet performs aggressive normalization of the PEM contents written into the pod filesystem.  Esoteric PEM features such as inter-block comments and block headers are stripped.  Certificates are deduplicated. The ordering of certificates within the file is arbitrary, and Kubelet may change the order over time.
         * 
         * @return builder
         * 
         */
        public Builder clusterTrustBundle(ClusterTrustBundleProjectionPatchArgs clusterTrustBundle) {
            return clusterTrustBundle(Output.of(clusterTrustBundle));
        }

        /**
         * @param configMap configMap information about the configMap data to project
         * 
         * @return builder
         * 
         */
        public Builder configMap(@Nullable Output<ConfigMapProjectionPatchArgs> configMap) {
            $.configMap = configMap;
            return this;
        }

        /**
         * @param configMap configMap information about the configMap data to project
         * 
         * @return builder
         * 
         */
        public Builder configMap(ConfigMapProjectionPatchArgs configMap) {
            return configMap(Output.of(configMap));
        }

        /**
         * @param downwardAPI downwardAPI information about the downwardAPI data to project
         * 
         * @return builder
         * 
         */
        public Builder downwardAPI(@Nullable Output<DownwardAPIProjectionPatchArgs> downwardAPI) {
            $.downwardAPI = downwardAPI;
            return this;
        }

        /**
         * @param downwardAPI downwardAPI information about the downwardAPI data to project
         * 
         * @return builder
         * 
         */
        public Builder downwardAPI(DownwardAPIProjectionPatchArgs downwardAPI) {
            return downwardAPI(Output.of(downwardAPI));
        }

        /**
         * @param podCertificate Projects an auto-rotating credential bundle (private key and certificate chain) that the pod can use either as a TLS client or server.
         * 
         * Kubelet generates a private key and uses it to send a PodCertificateRequest to the named signer.  Once the signer approves the request and issues a certificate chain, Kubelet writes the key and certificate chain to the pod filesystem.  The pod does not start until certificates have been issued for each podCertificate projected volume source in its spec.
         * 
         * Kubelet will begin trying to rotate the certificate at the time indicated by the signer using the PodCertificateRequest.Status.BeginRefreshAt timestamp.
         * 
         * Kubelet can write a single file, indicated by the credentialBundlePath field, or separate files, indicated by the keyPath and certificateChainPath fields.
         * 
         * The credential bundle is a single file in PEM format.  The first PEM entry is the private key (in PKCS#8 format), and the remaining PEM entries are the certificate chain issued by the signer (typically, signers will return their certificate chain in leaf-to-root order).
         * 
         * Prefer using the credential bundle format, since your application code can read it atomically.  If you use keyPath and certificateChainPath, your application must make two separate file reads. If these coincide with a certificate rotation, it is possible that the private key and leaf certificate you read may not correspond to each other.  Your application will need to check for this condition, and re-read until they are consistent.
         * 
         * The named signer controls chooses the format of the certificate it issues; consult the signer implementation&#39;s documentation to learn how to use the certificates it issues.
         * 
         * @return builder
         * 
         */
        public Builder podCertificate(@Nullable Output<PodCertificateProjectionPatchArgs> podCertificate) {
            $.podCertificate = podCertificate;
            return this;
        }

        /**
         * @param podCertificate Projects an auto-rotating credential bundle (private key and certificate chain) that the pod can use either as a TLS client or server.
         * 
         * Kubelet generates a private key and uses it to send a PodCertificateRequest to the named signer.  Once the signer approves the request and issues a certificate chain, Kubelet writes the key and certificate chain to the pod filesystem.  The pod does not start until certificates have been issued for each podCertificate projected volume source in its spec.
         * 
         * Kubelet will begin trying to rotate the certificate at the time indicated by the signer using the PodCertificateRequest.Status.BeginRefreshAt timestamp.
         * 
         * Kubelet can write a single file, indicated by the credentialBundlePath field, or separate files, indicated by the keyPath and certificateChainPath fields.
         * 
         * The credential bundle is a single file in PEM format.  The first PEM entry is the private key (in PKCS#8 format), and the remaining PEM entries are the certificate chain issued by the signer (typically, signers will return their certificate chain in leaf-to-root order).
         * 
         * Prefer using the credential bundle format, since your application code can read it atomically.  If you use keyPath and certificateChainPath, your application must make two separate file reads. If these coincide with a certificate rotation, it is possible that the private key and leaf certificate you read may not correspond to each other.  Your application will need to check for this condition, and re-read until they are consistent.
         * 
         * The named signer controls chooses the format of the certificate it issues; consult the signer implementation&#39;s documentation to learn how to use the certificates it issues.
         * 
         * @return builder
         * 
         */
        public Builder podCertificate(PodCertificateProjectionPatchArgs podCertificate) {
            return podCertificate(Output.of(podCertificate));
        }

        /**
         * @param secret secret information about the secret data to project
         * 
         * @return builder
         * 
         */
        public Builder secret(@Nullable Output<SecretProjectionPatchArgs> secret) {
            $.secret = secret;
            return this;
        }

        /**
         * @param secret secret information about the secret data to project
         * 
         * @return builder
         * 
         */
        public Builder secret(SecretProjectionPatchArgs secret) {
            return secret(Output.of(secret));
        }

        /**
         * @param serviceAccountToken serviceAccountToken is information about the serviceAccountToken data to project
         * 
         * @return builder
         * 
         */
        public Builder serviceAccountToken(@Nullable Output<ServiceAccountTokenProjectionPatchArgs> serviceAccountToken) {
            $.serviceAccountToken = serviceAccountToken;
            return this;
        }

        /**
         * @param serviceAccountToken serviceAccountToken is information about the serviceAccountToken data to project
         * 
         * @return builder
         * 
         */
        public Builder serviceAccountToken(ServiceAccountTokenProjectionPatchArgs serviceAccountToken) {
            return serviceAccountToken(Output.of(serviceAccountToken));
        }

        public VolumeProjectionPatchArgs build() {
            return $;
        }
    }

}
