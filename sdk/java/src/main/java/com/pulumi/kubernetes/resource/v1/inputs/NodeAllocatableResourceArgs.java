// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.kubernetes.resource.v1.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.kubernetes.resource.v1.inputs.NodeAllocatableMappingArgs;
import com.pulumi.kubernetes.resource.v1.inputs.NodeAllocatableOverheadArgs;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


/**
 * NodeAllocatableResource defines the translation between the DRA device/capacity units requested to the corresponding quantity of the node allocatable resource. At least one of Mapping or Overhead must be specified. Not specifying either is an invalid configuration.
 * 
 */
public final class NodeAllocatableResourceArgs extends com.pulumi.resources.ResourceArgs {

    public static final NodeAllocatableResourceArgs Empty = new NodeAllocatableResourceArgs();

    /**
     * Mapping is used when the device directly models a node allocatable resource like standard CPU or memory (e.g., with a CPU DRA driver). The calculated quantity is accounted for exactly once per claim instance on the node. To prevent node cgroup isolation friction, the scheduler explicitly blocks sharing mapped device claims across multiple pods.
     * 
     */
    @Import(name="mapping")
    private @Nullable Output<NodeAllocatableMappingArgs> mapping;

    /**
     * @return Mapping is used when the device directly models a node allocatable resource like standard CPU or memory (e.g., with a CPU DRA driver). The calculated quantity is accounted for exactly once per claim instance on the node. To prevent node cgroup isolation friction, the scheduler explicitly blocks sharing mapped device claims across multiple pods.
     * 
     */
    public Optional<Output<NodeAllocatableMappingArgs>> mapping() {
        return Optional.ofNullable(this.mapping);
    }

    /**
     * Overhead contains fields for modeling auxiliary overhead incurred on node allocatable resources when allocating devices that are not themselves modeling a node allocatable resource (e.g., host memory overhead for GPUs). Sharing overhead-mapped claims across multiple pods is allowed. The node allocatable overhead is accounted for individually for each pod referencing the claim. Overhead is always subtracted from the node&#39;s allocatable capacity for the resource, even when mapping is specified for the same resource. Eg: If a device models memory capacity per socket as a consumable capacity pool via Mapping (with CapacityKey), any overhead specified for the same resource will be subtracted from the node&#39;s general allocatable capacity and not from the per-socket capacity pool in Mapping.
     * 
     */
    @Import(name="overhead")
    private @Nullable Output<NodeAllocatableOverheadArgs> overhead;

    /**
     * @return Overhead contains fields for modeling auxiliary overhead incurred on node allocatable resources when allocating devices that are not themselves modeling a node allocatable resource (e.g., host memory overhead for GPUs). Sharing overhead-mapped claims across multiple pods is allowed. The node allocatable overhead is accounted for individually for each pod referencing the claim. Overhead is always subtracted from the node&#39;s allocatable capacity for the resource, even when mapping is specified for the same resource. Eg: If a device models memory capacity per socket as a consumable capacity pool via Mapping (with CapacityKey), any overhead specified for the same resource will be subtracted from the node&#39;s general allocatable capacity and not from the per-socket capacity pool in Mapping.
     * 
     */
    public Optional<Output<NodeAllocatableOverheadArgs>> overhead() {
        return Optional.ofNullable(this.overhead);
    }

    private NodeAllocatableResourceArgs() {}

    private NodeAllocatableResourceArgs(NodeAllocatableResourceArgs $) {
        this.mapping = $.mapping;
        this.overhead = $.overhead;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(NodeAllocatableResourceArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private NodeAllocatableResourceArgs $;

        public Builder() {
            $ = new NodeAllocatableResourceArgs();
        }

        public Builder(NodeAllocatableResourceArgs defaults) {
            $ = new NodeAllocatableResourceArgs(Objects.requireNonNull(defaults));
        }

        /**
         * @param mapping Mapping is used when the device directly models a node allocatable resource like standard CPU or memory (e.g., with a CPU DRA driver). The calculated quantity is accounted for exactly once per claim instance on the node. To prevent node cgroup isolation friction, the scheduler explicitly blocks sharing mapped device claims across multiple pods.
         * 
         * @return builder
         * 
         */
        public Builder mapping(@Nullable Output<NodeAllocatableMappingArgs> mapping) {
            $.mapping = mapping;
            return this;
        }

        /**
         * @param mapping Mapping is used when the device directly models a node allocatable resource like standard CPU or memory (e.g., with a CPU DRA driver). The calculated quantity is accounted for exactly once per claim instance on the node. To prevent node cgroup isolation friction, the scheduler explicitly blocks sharing mapped device claims across multiple pods.
         * 
         * @return builder
         * 
         */
        public Builder mapping(NodeAllocatableMappingArgs mapping) {
            return mapping(Output.of(mapping));
        }

        /**
         * @param overhead Overhead contains fields for modeling auxiliary overhead incurred on node allocatable resources when allocating devices that are not themselves modeling a node allocatable resource (e.g., host memory overhead for GPUs). Sharing overhead-mapped claims across multiple pods is allowed. The node allocatable overhead is accounted for individually for each pod referencing the claim. Overhead is always subtracted from the node&#39;s allocatable capacity for the resource, even when mapping is specified for the same resource. Eg: If a device models memory capacity per socket as a consumable capacity pool via Mapping (with CapacityKey), any overhead specified for the same resource will be subtracted from the node&#39;s general allocatable capacity and not from the per-socket capacity pool in Mapping.
         * 
         * @return builder
         * 
         */
        public Builder overhead(@Nullable Output<NodeAllocatableOverheadArgs> overhead) {
            $.overhead = overhead;
            return this;
        }

        /**
         * @param overhead Overhead contains fields for modeling auxiliary overhead incurred on node allocatable resources when allocating devices that are not themselves modeling a node allocatable resource (e.g., host memory overhead for GPUs). Sharing overhead-mapped claims across multiple pods is allowed. The node allocatable overhead is accounted for individually for each pod referencing the claim. Overhead is always subtracted from the node&#39;s allocatable capacity for the resource, even when mapping is specified for the same resource. Eg: If a device models memory capacity per socket as a consumable capacity pool via Mapping (with CapacityKey), any overhead specified for the same resource will be subtracted from the node&#39;s general allocatable capacity and not from the per-socket capacity pool in Mapping.
         * 
         * @return builder
         * 
         */
        public Builder overhead(NodeAllocatableOverheadArgs overhead) {
            return overhead(Output.of(overhead));
        }

        public NodeAllocatableResourceArgs build() {
            return $;
        }
    }

}
