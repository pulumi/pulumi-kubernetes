import pulumi
import pulumi.runtime

class {{Kind}}(pulumi.CustomResource):
    {{{Comment}}}
    def __init__(self, __name__, __opts__=None{{#Properties}}, {{Name}}=None{{/Properties}}):
        if not __name__:
            raise TypeError('Missing resource name argument (for URN creation)')
        if not isinstance(__name__, str):
            raise TypeError('Expected resource name to be a string')
        if __opts__ and not isinstance(__opts__, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')

        __props__ = dict()

        __props__['apiVersion'] = '{{RawAPIVersion}}'
        self.apiVersion = '{{RawAPIVersion}}'

        __props__['kind'] = '{{Kind}}'
        self.kind = '{{Kind}}'
        {{#RequiredProperties}}

        if not {{Name}}:
            raise TypeError('Missing required property {{Name}}')
        elif not isinstance({{Name}}, {{PropType}}):
            raise TypeError('Expected property aliases to be a {{PropType}}')
        self.{{Name}} = {{Name}}
        {{{Comment}}}
        __props__['{{Name}}'] = {{Name}}
        {{/RequiredProperties}}
        {{#OptionalProperties}}

        if {{Name}} and not isinstance({{Name}}, {{PropType}}):
            raise TypeError('Expected property aliases to be a {{PropType}}')
        self.{{Name}} = {{Name}}
        {{{Comment}}}
        __props__['{{Name}}'] = {{Name}}
        {{/OptionalProperties}}

        super({{Kind}}, self).__init__(
            "kubernetes:{{APIVersion}}:{{Kind}}",
            __name__,
            __props__,
            __opts__)
